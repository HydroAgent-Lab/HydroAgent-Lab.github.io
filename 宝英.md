
在 PowerShell 里用这两段。
关闭 3000 端口上的网页：

$ports = Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique
foreach ($procId in $ports) {
  Stop-Process -Id $procId -Force
}

cd E:\baoying_return\GitHub\hydroagent-lab\HydroAgent-Lab.github.io
$env:NODE_OPTIONS="--no-experimental-webstorage"
npm run dev

打开地址：
http://127.0.0.1:3000/
