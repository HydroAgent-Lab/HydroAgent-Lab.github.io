import { TeamPageContent } from "@/components/site-pages";

export const metadata = {
  title: "团队 | HydroAgent-Lab"
};

export default function TeamZhPage() {
  return <TeamPageContent lang="zh" />;
}
