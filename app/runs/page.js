import { RunsPageContent } from "@/components/site-pages";

export const metadata = {
  title: "Runs | HydroAgent-Lab"
};

export default function RunsPage() {
  return <RunsPageContent lang="en" />;
}
