import { TeamPageContent } from "@/components/site-pages";

export const metadata = {
  title: "Team | HydroAgent-Lab"
};

export default function TeamPage() {
  return <TeamPageContent lang="en" />;
}
