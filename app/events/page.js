import { EventsPageContent } from "@/components/pages/events";

export const metadata = {
  title: "Events | HydroAgent-Lab"
};

export default function EventsPage() {
  return <EventsPageContent lang="en" />;
}
